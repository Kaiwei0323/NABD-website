# Q&A JSON (default: developerQA.json next to main.py). Relative paths are from this folder.
# QA_JSON_PATH=developerQA.json

# Notebook-aligned models
# RETRIEVER_EMBEDDING_MODEL=nvidia/llama-3.2-nv-embedqa-1b-v2
# RETRIEVER_RERANK_MODEL=nvidia/llama-3.2-nv-rerankqa-1b-v2

# Match notebook: FAISS similarity k=6, reranker top_k=3
# FAISS_K=6
# RERANK_TOP_K=3

# Listen on all interfaces (recommended on Windows). Node still uses http://127.0.0.1:8000
HOST=0.0.0.0
PORT=8000
